# 🎯 Wishlist App

Красивое desktop-приложение для управления списком желаний с glassmorphism дизайном для macOS.

## ✨ Возможности

- 📝 **Управление товарами**: Добавляйте, редактируйте и удаляйте товары
- 💰 **Отслеживание цен**: Укажите цену и следите за общей суммой
- 🖼️ **Загрузка изображений**: Добавляйте фото товаров с вашего Mac
- 🔗 **Ссылки на товары**: Сохраняйте ссылки на интернет-магазины
- 📅 **Планирование покупок**: Устанавливайте желаемую дату покупки
- 🏷️ **Статусы**: Планируется, Куплено, Отложено
- 🔍 **Фильтрация и сортировка**: По цене, дате, статусу
- 🌓 **Автоматическая тема**: Переключается в зависимости от системной темы macOS
- 💾 **Локальное хранилище**: Все данные хранятся локально в IndexedDB
- 🎨 **Glassmorphism дизайн**: Современный минималистичный интерфейс

## 🛠 Технологии

- **Frontend**: React 18 + TypeScript + Vite
- **Backend**: Tauri (Rust)
- **База данных**: IndexedDB (через Dexie)
- **Стиль**: CSS с glassmorphism эффектами
- **Дата**: date-fns для форматирования

## 📋 Требования

Перед установкой убедитесь, что у вас установлены:

1. **Node.js** (v18 или выше)
   ```bash
   # Проверить версию
   node --version
   ```

2. **Rust** (последняя стабильная версия)
   ```bash
   # Установка через rustup
   curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
   
   # Проверить версию
   rustc --version
   ```

3. **Xcode Command Line Tools** (для macOS)
   ```bash
   xcode-select --install
   ```

## 🚀 Установка и запуск

### 1. Установка зависимостей

```bash
# Перейдите в директорию проекта
cd wishlist-app

# Установите npm зависимости
npm install
```

### 2. Запуск в режиме разработки

```bash
# Запустить приложение в режиме разработки
npm run tauri:dev
```

Это запустит приложение в окне macOS с hot reload - изменения в коде будут автоматически применяться.

### 3. Сборка production версии (.app)

```bash
# Собрать приложение для macOS
npm run tauri:build
```

После завершения сборки (это может занять несколько минут), готовое приложение будет находиться в:

```
src-tauri/target/release/bundle/macos/Wishlist.app
```

Вы можете:
- Запустить его двойным кликом
- Переместить в папку Applications
- Поделиться с другими пользователями Mac

## 📁 Структура проекта

```
wishlist-app/
├── src/                          # React frontend
│   ├── components/              # React компоненты
│   │   ├── WishlistCard.tsx    # Карточка товара
│   │   ├── WishlistModal.tsx   # Модальное окно добавления/редактирования
│   │   └── FilterBar.tsx       # Панель фильтров и сортировки
│   ├── db/                      # База данных
│   │   └── database.ts         # Настройка Dexie (IndexedDB)
│   ├── types/                   # TypeScript типы
│   │   └── wishlist.ts         # Типы данных
│   ├── App.tsx                  # Главный компонент
│   ├── App.css                  # Стили главного компонента
│   ├── main.tsx                 # Точка входа React
│   └── index.css                # Глобальные стили
├── src-tauri/                   # Tauri backend
│   ├── src/
│   │   └── main.rs             # Rust код
│   ├── Cargo.toml              # Зависимости Rust
│   ├── tauri.conf.json         # Конфигурация Tauri
│   └── icons/                  # Иконки приложения
├── package.json                # npm зависимости
├── tsconfig.json               # Конфигурация TypeScript
├── vite.config.ts              # Конфигурация Vite
└── index.html                  # HTML шаблон
```

## 🎨 Дизайн и темы

Приложение автоматически переключается между светлой и тёмной темой в зависимости от системных настроек macOS:

- **Светлая тема**: Фиолетово-синий градиент с белыми стеклянными элементами
- **Тёмная тема**: Тёмно-синий градиент с полупрозрачными элементами
- **Акцентный цвет**: Оранжевый (#ff6b35)

## 💾 Хранение данных

Все данные хранятся локально на вашем Mac в IndexedDB:

- Не требуется интернет-соединение
- Данные не передаются на серверы
- Приватность и безопасность гарантированы
- Данные сохраняются между запусками

## 🔧 Доступные команды

```bash
# Разработка
npm run dev              # Запустить Vite dev server
npm run tauri:dev        # Запустить Tauri в режиме разработки

# Сборка
npm run build            # Собрать React приложение
npm run tauri:build      # Собрать Tauri приложение (.app)

# Предпросмотр production сборки
npm run preview          # Предпросмотр сборки Vite
```

## 📝 Использование

### Добавление товара

1. Нажмите кнопку **"➕ Добавить товар"**
2. Заполните форму:
   - Название (обязательно)
   - Цена (обязательно)
   - Ссылка на товар (опционально)
   - Изображение (опционально)
   - Описание (опционально)
   - Желаемая дата покупки (опционально)
   - Статус (по умолчанию "Планируется")
3. Нажмите **"Добавить"**

### Фильтрация и сортировка

- **По статусу**: Кликните на чипы статусов
- **По цене**: Установите минимальную и максимальную цену
- **Сортировка**: Выберите поле и направление сортировки

### Изменение статуса

- Используйте выпадающий список на карточке товара
- Или откройте форму редактирования

## 🐛 Решение проблем

### Ошибка при сборке Tauri

Убедитесь, что установлены все зависимости:

```bash
# Обновить Rust
rustup update

# Переустановить зависимости npm
rm -rf node_modules
npm install
```

### Не работает загрузка изображений

Убедитесь, что в `tauri.conf.json` настроены правильные разрешения для файловой системы.

### Приложение не открывается

Проверьте консоль на ошибки:

```bash
# Запустить в режиме разработки с логами
npm run tauri:dev
```

## 📦 Генерация иконок

Для генерации иконок приложения используйте:

```bash
# Установить tauri-cli глобально
npm install -g @tauri-apps/cli

# Сгенерировать иконки из исходного файла
tauri icon path/to/your/icon.png
```

Иконка должна быть квадратной, минимум 512x512px.

## 🔄 Обновление зависимостей

```bash
# Проверить устаревшие пакеты
npm outdated

# Обновить все зависимости
npm update

# Обновить Tauri CLI
npm install -g @tauri-apps/cli@latest
```

## 📄 Лицензия

MIT

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте раздел "Решение проблем"
2. Посмотрите [документацию Tauri](https://tauri.app)
3. Проверьте логи в консоли разработчика

## 🎯 Будущие улучшения

- [ ] Экспорт/импорт данных
- [ ] Категории товаров
- [ ] Статистика покупок
- [ ] Уведомления о желаемых датах
- [ ] Интеграция с облачным хранилищем
- [ ] Поддержка нескольких списков

---

Создано с ❤️ для macOS
