# 🚀 Быстрый старт

## Шаг 1: Установка требований

### Node.js
```bash
# Скачать с https://nodejs.org (v18+)
# Или через Homebrew:
brew install node
```

### Rust
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

### Xcode Command Line Tools
```bash
xcode-select --install
```

## Шаг 2: Установка зависимостей

```bash
cd wishlist-app
npm install
```

## Шаг 3: Запуск

### Режим разработки
```bash
npm run tauri:dev
```

### Сборка .app для macOS
```bash
npm run tauri:build
```

Готовое приложение будет в:
`src-tauri/target/release/bundle/macos/Wishlist.app`

## 🎨 Что внутри?

- ✅ React + TypeScript + Vite
- ✅ Tauri для создания native macOS приложения
- ✅ IndexedDB для локального хранения данных
- ✅ Glassmorphism дизайн
- ✅ Автоматическая тема (светлая/тёмная)
- ✅ Офлайн работа
- ✅ Полный CRUD для товаров
- ✅ Фильтры и сортировка

## 📱 Основные функции

- Добавление товаров с фото, ценой, ссылками
- Установка желаемой даты покупки
- Статусы: Планируется / Куплено / Отложено
- Фильтрация по цене и статусу
- Сортировка по разным полям
- Подсчёт общей суммы планируемых покупок

## ❓ Проблемы?

1. **Ошибка компиляции Rust**: `rustup update`
2. **Ошибка npm**: `rm -rf node_modules && npm install`
3. **Не открывается**: Проверьте логи в `npm run tauri:dev`

Полная документация в README.md
