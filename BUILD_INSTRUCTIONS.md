# 📦 ИНСТРУКЦИЯ ПО СБОРКЕ И ЗАПУСКУ

## 🎯 Вы находитесь здесь

Перед вами полноценное desktop-приложение Wishlist для macOS. Структура проекта готова, все файлы созданы. Осталось только установить зависимости и запустить!

## ⚡ Быстрый старт (3 шага)

### Шаг 1: Проверка требований

```bash
cd /Users/mishana/PycharmProjects/wishlist-app
./check-requirements.sh
```

Этот скрипт проверит, что у вас установлены:
- Node.js v18+
- Rust (rustc + cargo)
- Xcode Command Line Tools

Если чего-то не хватает, следуйте инструкциям скрипта.

### Шаг 2: Установка зависимостей

```bash
npm install
```

Это установит все необходимые npm пакеты (~200MB). Займёт 2-5 минут.

### Шаг 3: Запуск приложения

#### Режим разработки (с hot reload):
```bash
npm run tauri:dev
```

При первом запуске Rust скомпилирует Tauri (займёт 5-10 минут). 
В следующие разы запуск будет быстрым (5-10 секунд).

#### Сборка финального .app:
```bash
npm run tauri:build
```

Готовое приложение будет в:
```
src-tauri/target/release/bundle/macos/Wishlist.app
```

## 📋 Что если требований нет?

### Установка Node.js (v18+)

**Вариант 1: Homebrew (рекомендуется)**
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install node
```

**Вариант 2: Официальный сайт**
Скачайте с https://nodejs.org (выберите LTS версию)

### Установка Rust

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
source $HOME/.cargo/env
```

После установки проверьте:
```bash
rustc --version
cargo --version
```

### Установка Xcode Command Line Tools

```bash
xcode-select --install
```

Появится диалоговое окно - нажмите "Установить".

## 🎨 Что внутри приложения?

После запуска вы увидите:

1. **Главный экран** с кнопкой "Добавить товар"
2. **Статистику**: количество товаров и общую сумму
3. **Фильтры**: по статусу, цене, дате
4. **Сортировку**: по цене, дате, названию

### Добавление товара:

1. Нажмите "➕ Добавить товар"
2. Заполните:
   - Название (обязательно)
   - Цену (обязательно)
   - Ссылку на товар
   - Загрузите изображение (через кнопку 📷)
   - Добавьте описание
   - Установите желаемую дату покупки
3. Нажмите "Добавить"

### Карточка товара показывает:

- Изображение товара (если загружено)
- Название и цена
- Статус (планируется/куплено/отложено)
- Описание
- Дату желаемой покупки
- Ссылку на товар
- Кнопки редактирования и удаления

## 🎨 Темы

Приложение автоматически переключается между светлой и тёмной темой в зависимости от системных настроек macOS:

**Проверить текущую тему:**
System Preferences → General → Appearance → Light/Dark/Auto

## 💾 Где хранятся данные?

Все данные хранятся локально в IndexedDB браузера:
```
~/Library/Application Support/com.wishlist.app/
```

Данные:
- ✅ Приватные (не уходят в интернет)
- ✅ Сохраняются между запусками
- ✅ Не требуют авторизации
- ✅ Работают офлайн

## 🔧 Доступные команды

```bash
# Разработка
npm run dev              # Только Vite (без Tauri)
npm run tauri:dev        # Полное приложение с hot reload

# Сборка
npm run build            # Собрать React
npm run tauri:build      # Собрать .app для macOS

# Проверка
./check-requirements.sh  # Проверить установленные зависимости
npm run preview          # Предпросмотр production сборки
```

## 🐛 Решение проблем

### "command not found: npm"
Установите Node.js (см. выше)

### "command not found: rustc"
Установите Rust (см. выше)

### "xcrun: error: invalid active developer path"
Установите Xcode Command Line Tools: `xcode-select --install`

### Приложение не компилируется
```bash
# Очистить кеш и переустановить
rm -rf node_modules
rm -rf src-tauri/target
npm install
```

### Ошибки TypeScript
```bash
# Проверить типы
npx tsc --noEmit
```

### Долгая первая компиляция Rust
Это нормально! Первая компиляция Tauri занимает 5-10 минут.
Следующие сборки будут быстрыми.

### Не загружаются изображения
Убедитесь, что:
1. Выбран файл формата PNG/JPG/JPEG/GIF/WEBP
2. Файл не повреждён
3. У приложения есть доступ к файловой системе

## 📸 Генерация иконок (опционально)

Если хотите свою иконку:

```bash
# Установить Tauri CLI глобально
npm install -g @tauri-apps/cli

# Сгенерировать иконки из изображения (минимум 512x512px)
tauri icon path/to/your/icon.png
```

Подробнее в `ICONS.md`

## 📚 Документация

- `README.md` - Полная документация
- `QUICKSTART.md` - Быстрый старт
- `DEV_NOTES.md` - Заметки для разработчиков
- `ICONS.md` - Генерация иконок

## 🎯 Следующие шаги

1. ✅ Создана структура проекта
2. ✅ Написан весь код
3. ⏭️ **Установить зависимости: `npm install`**
4. ⏭️ **Запустить: `npm run tauri:dev`**
5. ⏭️ Собрать .app: `npm run tauri:build`

## 💡 Советы

- Используйте `npm run tauri:dev` для разработки (быстрый перезапуск)
- Используйте `npm run tauri:build` для финальной сборки
- Первая сборка долгая - наберитесь терпения
- Все изменения в коде применяются автоматически в dev режиме
- Данные сохраняются автоматически

## ✨ Фичи приложения

- [x] Добавление/редактирование/удаление товаров
- [x] Загрузка изображений с Mac
- [x] Цена и подсчёт общей суммы
- [x] Ссылки на товары
- [x] Описание товаров
- [x] Желаемая дата покупки
- [x] Статусы (планируется/куплено/отложено)
- [x] Фильтрация по статусу и цене
- [x] Сортировка по разным полям
- [x] Автоматическая тема (светлая/тёмная)
- [x] Glassmorphism дизайн
- [x] Локальное хранилище (IndexedDB)
- [x] Офлайн работа
- [x] Native macOS приложение (.app)

---

## 🚀 ГОТОВ К ЗАПУСКУ!

```bash
cd /Users/mishana/PycharmProjects/wishlist-app
./check-requirements.sh
npm install
npm run tauri:dev
```

Удачи! 🎉
